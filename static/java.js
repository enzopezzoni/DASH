
function show(id) {
    const elem = document.querySelector(id);
    elem.style.display = "inline";
}

function hide(id) {
    const elem = document.querySelector(id);
    elem.style.display = "none";
}